# Facilities Module (Member 1)

Use this package for resource and resource-type development.

Start with:
1. `model/ResourceType.java`
2. `repository/ResourceTypeRepository.java`
3. `service/ResourceService` enhancements for CRUD/filtering
4. `controller` endpoints for create, update, delete, and filters
