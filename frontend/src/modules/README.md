# Frontend Team Module Structure

This folder separates work by team responsibility:

- `facilities` (Member 1)
- `tickets` (Member 3)
- `auth` (Member 4)

Each module contains:
- `pages`
- `components`
- `services`

Member 2 booking pages remain in existing paths under `src/pages` and `src/components/bookings`.
